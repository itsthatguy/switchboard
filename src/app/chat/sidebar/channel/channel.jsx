/* global React */
export default React.createClass({
  componentWillMount () {
    // debugger;
  },

  componentDidMount () {
    // debugger;
  },

  componentWillUpdate () {
    // debugger;
  },

  componentDidUpdate () {
    // debugger;
  },

  render () {
    return (
      <div className='channel__wrapper'>
        {this.props.data.id}
      </div>
    );
  }
});
